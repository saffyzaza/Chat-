# 📋 คู่มือการใช้งานเครื่องมือ (Tools Usage Guide)

## 🎯 หลักการทำงาน

ระบบจะตรวจสอบ 3 เงื่อนไข:
1. **มีเครื่องมือที่เลือกหรือไม่** (`selectedTool`)
2. **มีไฟล์แนบหรือไม่** (`files.length > 0`)
3. **ผู้ใช้พิมพ์ prompt เองหรือไม่** (`prompt.trim() !== ""`)

---

## 📊 ตารางการทำงาน

| สถานการณ์ | เลือกเครื่องมือ | แนบไฟล์ | พิมพ์ข้อความ | Prompt ที่ส่งไป | หมายเหตุ |
|-----------|:---------------:|:-------:|:-----------:|----------------|---------|
| **1️⃣** | ✅ ค้นหาข้อมูล | ✅ รูป | ❌ | "ช่วยค้นหาข้อมูลเกี่ยวกับสุขภาพให้หน่อย **จากไฟล์/รูปภาพที่แนบมา**" | ✨ ต่อท้าย |
| **2️⃣** | ✅ เปรียบเทียบ | ✅ PDF | ❌ | "ช่วยเปรียบเทียบข้อมูลด้านสุขภาพให้หน่อย **จากไฟล์/รูปภาพที่แนบมา**" | ✨ ต่อท้าย |
| **3️⃣** | ✅ สรุปรายงาน | ❌ | ❌ | "ช่วยสรุปรายงานด้านสุขภาพให้หน่อย" | ไม่ต่อท้าย |
| **4️⃣** | ✅ สร้างกราฟ | ✅ Excel | ❌ | "ช่วยอธิบายข้อมูลในรูปแบบที่เข้าใจง่าย **จากไฟล์/รูปภาพที่แนบมา**" | ✨ ต่อท้าย |
| **5️⃣** | ❌ | ✅ รูป | ✅ "วิเคราะห์นี้" | "วิเคราะห์นี้" | ใช้ที่พิมพ์ |
| **6️⃣** | ❌ | ❌ | ✅ "สวัสดี" | "สวัสดี" | แชทปกติ |
| **7️⃣** | ✅ ขอคำปรึกษา | ✅ รูป 2 | ✅ "ช่วยดูหน่อย" | "ช่วยดูหน่อย" | ผู้ใช้พิมพ์เอง |

---

## 🔍 รายละเอียดแต่ละสถานการณ์

### 1️⃣ **เลือกเครื่องมือ + แนบไฟล์ (ไม่พิมพ์)**
```
ผู้ใช้:
  ✅ เลือก "ค้นหาข้อมูล"
  ✅ แนบรูปภาพผลเลือด
  ❌ ไม่พิมพ์อะไร
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "ช่วยค้นหาข้อมูลเกี่ยวกับสุขภาพให้หน่อย จากไฟล์/รูปภาพที่แนบมา"
```

### 2️⃣ **เลือกเครื่องมือ + แนบ PDF (ไม่พิมพ์)**
```
ผู้ใช้:
  ✅ เลือก "เปรียบเทียบข้อมูล"
  ✅ แนบไฟล์ PDF สองฉบับ
  ❌ ไม่พิมพ์อะไร
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "ช่วยเปรียบเทียบข้อมูลด้านสุขภาพให้หน่อย จากไฟล์/รูปภาพที่แนบมา"
```

### 3️⃣ **เลือกเครื่องมือ + ไม่แนบไฟล์ (ไม่พิมพ์)**
```
ผู้ใช้:
  ✅ เลือก "สรุปรายงาน"
  ❌ ไม่แนบไฟล์
  ❌ ไม่พิมพ์อะไร
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "ช่วยสรุปรายงานด้านสุขภาพให้หน่อย"
  
⚠️ หมายเหตุ: ไม่ต่อท้าย "จากไฟล์..." เพราะไม่มีไฟล์
```

### 4️⃣ **เลือกเครื่องมือ + แนบไฟล์ Excel (ไม่พิมพ์)**
```
ผู้ใช้:
  ✅ เลือก "สร้างกราฟ"
  ✅ แนบไฟล์ Excel (data.xlsx)
  ❌ ไม่พิมพ์อะไร
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "ช่วยอธิบายข้อมูลในรูปแบบที่เข้าใจง่าย จากไฟล์/รูปภาพที่แนบมา"
```

### 5️⃣ **ไม่เลือกเครื่องมือ + แนบไฟล์ + พิมพ์เอง**
```
ผู้ใช้:
  ❌ ไม่เลือกเครื่องมือ
  ✅ แนบรูปภาพ
  ✅ พิมพ์: "วิเคราะห์ข้อมูลในรูปนี้หน่อย"
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "วิเคราะห์ข้อมูลในรูปนี้หน่อย"
  
✅ ใช้ prompt ที่ผู้ใช้พิมพ์เอง ไม่ต่อท้ายอะไร
```

### 6️⃣ **แชทปกติ (ไม่มีเครื่องมือ ไม่มีไฟล์)**
```
ผู้ใช้:
  ❌ ไม่เลือกเครื่องมือ
  ❌ ไม่แนบไฟล์
  ✅ พิมพ์: "สวัสดีครับ"
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "สวัสดีครับ"
```

### 7️⃣ **เลือกเครื่องมือ + แนบไฟล์ + แต่พิมพ์เอง**
```
ผู้ใช้:
  ✅ เลือก "ขอคำปรึกษา" (แต่จะไม่ใช้)
  ✅ แนบรูป 2 รูป
  ✅ พิมพ์: "ช่วยดูอาการในรูปหน่อย ผมเป็นอะไรหรือเปล่า"
  ➡️ กดส่ง

Prompt ที่ส่ง:
  "ช่วยดูอาการในรูปหน่อย ผมเป็นอะไรหรือเปล่า"
  
✅ ผู้ใช้พิมพ์เอง = ใช้ prompt ที่พิมพ์ (ไม่สนเครื่องมือ)
```

---

## 🛠️ รายการเครื่องมือทั้งหมด

| ไอคอน | ชื่อเครื่องมือ | Prompt เริ่มต้น | ใช้งานเมื่อ |
|:-----:|---------------|----------------|------------|
| 🔍 | **ค้นหาข้อมูล** | "ช่วยค้นหาข้อมูลเกี่ยวกับสุขภาพให้หน่อย" | ต้องการค้นหาข้อมูลจากไฟล์ |
| ⚖️ | **เปรียบเทียบข้อมูล** | "ช่วยเปรียบเทียบข้อมูลด้านสุขภาพให้หน่อย" | เปรียบเทียบ 2 ไฟล์ขึ้นไป |
| 💬 | **ขอคำปรึกษา** | "ต้องการคำปรึกษาเกี่ยวกับสุขภาพ" | ปรึกษาเรื่องสุขภาพ |
| 📄 | **สรุปรายงาน** | "ช่วยสรุปรายงานด้านสุขภาพให้หน่อย" | สรุป PDF/เอกสารยาวๆ |
| 📊 | **สร้างกราฟ** | "ช่วยอธิบายข้อมูลในรูปแบบที่เข้าใจง่าย" | แปลงข้อมูลเป็นกราฟ |
| 📝 | **เขียนแผนงาน** | "ช่วยวางแผนการดูแลสุขภาพให้หน่อย" | วางแผนการดูแลสุขภาพ |

---

## 💡 ตัวอย่างการใช้งานจริง

### ตัวอย่างที่ 1: วิเคราะห์ผลเลือด
```
ขั้นตอน:
1. กดปุ่ม "เครื่องมือ"
2. เลือก "สรุปรายงาน"
3. แนบรูป "ใบผลเลือด.jpg"
4. กดส่ง (ไม่ต้องพิมพ์)

ผลลัพธ์:
✅ AI จะได้รับ:
   - Prompt: "ช่วยสรุปรายงานด้านสุขภาพให้หน่อย จากไฟล์/รูปภาพที่แนบมา"
   - รูป: ใบผลเลือด.jpg
   
✅ AI จะตอบ:
   "จากรูปภาพผลเลือดที่คุณส่งมา ผมขอสรุปดังนี้..."
```

### ตัวอย่างที่ 2: เปรียบเทียบสองไฟล์
```
ขั้นตอน:
1. กดปุ่ม "เครื่องมือ"
2. เลือก "เปรียบเทียบข้อมูล"
3. แนบไฟล์ "รายงาน_2023.pdf"
4. แนบไฟล์ "รายงาน_2024.pdf"
5. กดส่ง (ไม่ต้องพิมพ์)

ผลลัพธ์:
✅ AI จะได้รับ:
   - Prompt: "ช่วยเปรียบเทียบข้อมูลด้านสุขภาพให้หน่อย จากไฟล์/รูปภาพที่แนบมา"
   - ไฟล์ 2 ไฟล์
   
✅ AI จะตอบ:
   "จากเอกสาร PDF ทั้งสองฉบับที่คุณส่งมา ผมจะเปรียบเทียบให้ดังนี้..."
   [แสดงตารางเปรียบเทียบ]
```

### ตัวอย่างที่ 3: สร้างกราฟจาก Excel
```
ขั้นตอน:
1. กดปุ่ม "เครื่องมือ"
2. เลือก "สร้างกราฟ"
3. แนบไฟล์ "ยอดขาย.xlsx"
4. กดส่ง (ไม่ต้องพิมพ์)

ผลลัพธ์:
✅ AI จะได้รับ:
   - Prompt: "ช่วยอธิบายข้อมูลในรูปแบบที่เข้าใจง่าย จากไฟล์/รูปภาพที่แนบมา"
   - ไฟล์: ยอดขาย.xlsx
   
✅ AI จะตอบ:
   "จากไฟล์ Excel ที่แนบมา ผมได้สร้างกราฟให้แล้วครับ..."
   [แสดงกราฟ Chart.js]
```

### ตัวอย่างที่ 4: พิมพ์เองแบบละเอียด
```
ขั้นตอน:
1. แนบรูป "อาการปวดหลัง.jpg"
2. พิมพ์: "หมอครับ ผมปวดหลังมา 3 วัน ช่วยดูรูปและแนะนำหน่อย"
3. กดส่ง

ผลลัพธ์:
✅ AI จะได้รับ:
   - Prompt: "หมอครับ ผมปวดหลังมา 3 วัน ช่วยดูรูปและแนะนำหน่อย"
   - รูป: อาการปวดหลัง.jpg
   
✅ AI จะตอบ:
   "จากรูปภาพและอาการที่คุณบอก ผมขอแนะนำดังนี้..."
```

---

## ✨ สรุป Logic การทำงาน (Code Flow)

```javascript
function handleSubmit() {
  let finalPrompt = prompt.trim();
  const hasFiles = files.length > 0;
  const userTypedOwnPrompt = finalPrompt !== "";
  
  // เคส 1: เลือกเครื่องมือ + ไม่พิมพ์เอง
  if (selectedTool && !userTypedOwnPrompt) {
    finalPrompt = TOOL_PROMPTS[selectedTool]; // เช่น "ช่วยค้นหาข้อมูล..."
    
    // ถ้ามีไฟล์ → ต่อท้าย
    if (hasFiles) {
      finalPrompt += " จากไฟล์/รูปภาพที่แนบมา";
    }
  }
  // เคส 2: ผู้ใช้พิมพ์เอง → ใช้ prompt ที่พิมพ์ (ไม่แตะ)
  
  // ส่ง prompt + images + files
  onSend(finalPrompt, imageUrls, files);
}
```

---

## 🎯 ข้อดีของระบบนี้

✅ **ยืดหยุ่น**: รองรับทั้งการใช้เครื่องมือและพิมพ์เอง  
✅ **ชาญฉลาด**: ต่อท้าย "จากไฟล์..." เฉพาะเมื่อจำเป็น  
✅ **ไม่รบกวน**: ถ้าผู้ใช้พิมพ์เอง จะไม่แตะต้อง prompt  
✅ **สะดวก**: กดส่งได้เลย ไม่ต้องพิมพ์ซ้ำๆ  

---

## 📌 หมายเหตุสำคัญ

1. ⚠️ **ลำดับความสำคัญ**: ถ้าผู้ใช้พิมพ์เอง → ระบบจะใช้ที่พิมพ์ (ไม่สนเครื่องมือ)
2. ✅ **การต่อท้าย**: เกิดขึ้นเฉพาะเมื่อ `selectedTool + hasFiles + !userTyped`
3. 🎨 **UI**: ปุ่ม Send จะ enable เมื่อมี `prompt` หรือ `selectedTool` หรือ `files`
4. 🧹 **Clean Up**: หลังส่ง จะเคลียร์ prompt, files, previews, selectedTool

---

สร้างเมื่อ: 18 พฤศจิกายน 2025  
อัปเดตล่าสุด: 18 พฤศจิกายน 2025  
เวอร์ชัน: 1.0.0
